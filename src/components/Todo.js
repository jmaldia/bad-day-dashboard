import React from 'react'

const TextDecodeOptions = (props) => {
    return (
        <div style={styles}>
        
        </div>
    );
};

const styles = {
    
};

export default TextDecodeOptions;